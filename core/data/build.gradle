plugins {
    id 'com.android.library'
    id 'org.jetbrains.kotlin.android'
}

android {
    namespace 'jp.mydns.kokoichi0206.data'
    compileSdk build_versions.target_sdk
}

dependencies {
    implementation deps.retrofit.gson

    // modules
    implementation project(":core:model")
}
